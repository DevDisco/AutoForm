    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="{{id}}">{{label}}</label>
        <div class="col-sm-9">
            <input class="form-control" id="{{id}}" type="number" name="{{name}}" value="{{value}}" max="{{max}}" title="{{instructions}}" {{required}}>
        </div>
    </div>